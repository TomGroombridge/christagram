<% if user_signed_in? %>
  <div class="container">
    <div class="row">
      <% @posts.each do |post| %>
        <div class="col-md-3">
        <div class="thumbnail" style= "min-height: 500px">
          <%= image_tag post.image.url(:thumb), class: "christagram-upload", style: "height:250px; width:250px" %>
          <div class="caption">
          <h3> <%= post.title %> </h3>
          <h4> <%= post.content %> </h4>
                    <p>
          <% post.tags.each do |tag| %>
          <%= tag %>
          <% end %></p>
          <h5> Created <%= time_ago_in_words post.created_at %> ago </h5>
           <button><%= link_to "delete", post_path(post), :method => :delete, data: { confirm: 'Are you sure?' } %></button>
           <button><%= link_to "edit", edit_post_path(post) %></button>
          </div>
          </div>   
        </div>
      <% end %>
    </div>
  </div>
<% end %>